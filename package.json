{
  "name": "wapi",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "compat"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "bun test",
    "contracts:generate": "bun run packages/contracts/src/generate.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "dependencies": {
    "@uploadx-sdk/core": "^0.3.1"
  }
}