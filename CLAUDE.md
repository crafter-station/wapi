See [AGENTS.md](AGENTS.md).

It is the single source for repo conventions, the fidelity contract, deployment, and the traps
worth knowing before changing anything. This file exists only so Claude Code loads it; keep the
content in `AGENTS.md` rather than duplicating it here, so every agent reads the same thing.
